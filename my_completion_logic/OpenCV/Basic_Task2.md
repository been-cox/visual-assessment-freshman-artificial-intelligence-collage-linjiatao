通过创建滑动条和回调函数，当滑动滑动条时，回调函数会被调用，重新设置亮度和曝光度；值得注意的是createTrackbar函数的最大值默认大于等于0，而曝光度是负数，因此需要后续对参数的大小进行修改；这里采用'M', 'J', 'P', 'G'的编码格式，能对每一帧进行压缩；在while外，我们设置了	is_record的bool变量，通过按r改变这个布尔变量，在循环中，只有它为true的时候，我们才将该帧保存，实现了将通过按r控制开始录制和停止录制；同时，录制时画面中会有一个红色的实心圆，表明当前是录制状态。