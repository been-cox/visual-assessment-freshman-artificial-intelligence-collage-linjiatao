本任务分为两个程序，程序1是拍照保存棋盘格照片，程序2则是通过程序1获得的棋盘格照片实现相机标定；程序2假设棋盘格位于世界坐标的Z=0平面上，根据设置的格子大小，获得棋盘格角点的世界坐标；通过findChessboardCorners函数获得内角点的像素坐标，并通过cornerSubPix函数提升坐标的精度；当一组照片的像素坐标被找到时，我们便把世界坐标和像素坐标都存入容器中，最后通过calibrateCamera函数根据多组对应的世界坐标和像素坐标获得外参矩阵和畸变参数。