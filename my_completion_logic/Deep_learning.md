Task1：使用letnet5实现手写数字分类这一任务，我只能说是把模型跑成功了，因为模型训练代码我是直接复制Gemini的，训练完的模型能成功识别手写数字，不过没有去仔细研究，也便没有把代码放在src文件夹里；
Task2：yolo目标检测，我把蜘蛛侠作为检测对象，在网上找了五十多张照片，手动打标，然后通过数据集增强，得到170多张照片，便开始训练模型，第一次训练模型时用CPU训练，50轮跑了一个多小时，当时等不了便去上课，回来发现电脑网络断了，重新连接刷新界面后，训练得到的模型已经丢失，只好再训练一遍，用GPU训练了30轮，只用了两分钟，后面用训练完的模型去测试，只能说效果还行，但不够好，有时并不能识别出蜘蛛侠，估计时数据太少了，因为我只找了五十多张照片，不过总体效果还是挺满意的；
Task3：由于能力有限，当前的知识储备还不能让我阅读该篇论文，只好把论文扔给Gemini，让AI用易懂的语言解释给我听，效果图文件夹里的笔记便是我对论文的全部理解，如有错误，希望师兄多多指教。